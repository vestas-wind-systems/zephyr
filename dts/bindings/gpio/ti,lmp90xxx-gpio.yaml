title: TI LMP90xxx AFE GPIO controller

description: TI LMP90xxx GPIO controller binding

compatible: "ti,lmp90xxx-gpio"

include: [gpio-controller.yaml, base.yaml]

parent-bus: lmp90xxx

properties:
    label:
      required: true

    "#gpio-cells":
      const: 2

gpio-cells:
  - pin
  - flags

description: Renesas RCAR CMT timer

compatible: "rcar,cmt"

include: base.yaml

properties:
    label:
      required: true

    reg:
      required: true

    clock-frequency:
      type: int
      required: true
      description: Clock frequency information for Timer operation
